# Git Refresh Test
row 2

new line for new branch 
## Branch Updates
git clone for clonning new remote repo to local which is not available in local
git pull for pulling a repo from remote which is already available in local and to get synced in local
echo space text to edit a file 
git commit -m and write message
git push origin main to push the changes directly to remote repo
git checkout -b and branch name for creating and moving to that branch
the commit and push as usual
